# Add the following line to your ~/.cuda-gdbinit if you are debugging binaries built in a container
# and running them on the host. This helps cuda-gdb find the correct source files when absolute paths differ.
# Example: If your source is at /app inside the container but at /path/to/project on the host,
# update the paths accordingly.

set substitute-path /app /path/to/project/